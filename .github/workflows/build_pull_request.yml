name: PR Build Check

on:
  pull_request:
    branches:
      - master

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build changed extensions
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Write CI gradle.properties
        run: |
          mkdir -p ~/.gradle
          cat > ~/.gradle/gradle.properties << 'EOF'
          org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=512m
          org.gradle.daemon=false
          org.gradle.parallel=true
          org.gradle.configureondemand=true
          kotlin.incremental=false
          EOF

      - name: Detect changed extension modules
        id: changed
        run: |
          MODULES=$(git diff --name-only origin/master...HEAD \
            | grep '^src/' \
            | awk -F'/' '{print $2"."$3}' \
            | sort -u \
            | tr '\n' ' ')
          echo "modules=$MODULES" >> $GITHUB_OUTPUT
          echo "Changed: $MODULES"

      - name: Build changed extensions
        if: ${{ steps.changed.outputs.modules != '' }}
        run: |
          for MOD in ${{ steps.changed.outputs.modules }}; do
            echo "::group::Building $MOD"
            ./gradlew :src:${MOD}:assembleDebug
            echo "::endgroup::"
          done

      - name: No extension changes detected
        if: ${{ steps.changed.outputs.modules == '' }}
        run: echo "No extension source files changed in this PR — nothing to build."
        
